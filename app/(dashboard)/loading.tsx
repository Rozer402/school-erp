import LoadingSkeleton from "@/components/shared/LoadingSkeleton";export default function DashboardLoading() {  return (    <div className="p-8">      <LoadingSkeleton count={4} className="mb-8" />      <div className="grid grid-cols-1 md:grid-cols-3 gap-6">        <LoadingSkeleton count={1} />        <LoadingSkeleton count={1} />        <LoadingSkeleton count={1} />      </div>    </div>  );}
